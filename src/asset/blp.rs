// TODO: Load BLP textures using image-blp crate and convert to Bevy Image

use bevy::prelude::*;

pub struct BlpTexturePlugin;

impl Plugin for BlpTexturePlugin {
    fn build(&self, _app: &mut App) {}
}
